{
  "name": "@glmachado/any-store",
  "private": false,
  "version": "0.5.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "module": "./dist/lib.js",
  "types": "./dist/src/lib.d.ts",
  "exports": {
    ".": {
      "import": "./dist/lib.es.js",
      "types": "./dist/src/lib.d.ts"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run check && npm run wasm && vite build",
    "preview": "vite preview",
    "check": "tsc --build",
    "test": "vitest",
    "bench": "vitest bench",
    "test:browser": "vitest --config=vitest.browser.config.ts",
    "test-ci": "npm run wasm && vitest --run & vitest --config=vitest.browser.config.ts --run --browser.headless & cargo test",
    "wasm": "cargo wasm && wasm-bindgen --target web --out-dir ./pkg ../target/wasm32-unknown-unknown/release/any_store.wasm",
    "deploy": "npm run build && npm run test-ci && npm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@vitest/browser-playwright": "^4.0.18",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3",
    "vite": "npm:rolldown-vite@7.2.5",
    "vite-plugin-dts": "^4.5.4",
    "@glmachado/any-store": "^0.1.1",
    "vitest": "^4.0.16"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  },
  "dependencies": {}
}
